enum AssetStatus {
  ISSUED
  BURNED
  TRANSFERRED
}

type Asset @entity {
  id: ID!
  status: AssetStatus!
}

type AssetOperation @entity {
  id: ID!
  asset: Asset!
  to: String
  from: String
  amount: BigInt!
}